# YAML View

Press `y` on any resource to view its full YAML definition with syntax highlighting.

The configuration key `show_managed_fields` in `[general]` controls whether `managedFields` are included in the output.
